[package]
name = "toytest"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
bindgen = "0.69"

[dependencies]
libc = "0.2"

lazy_static = "1.4"